playwright>=1.40
pytest>=8
markdown>=3.5

# Optional: spoken-audio transcription for video-aware analysis. install.sh
# tries to install this separately; if it has no wheel for your Python version
# the brief simply runs frames-only. By hand: pip install faster-whisper
# faster-whisper>=1.0
